name: jornada_superendividamento
version: '1.0.0'
config-version: 2
profile: jornada_superendividamento
model-paths: [models]
clean-targets: [target, dbt_packages]
vars:
  piso_minimo_existencial: 1200
models:
  jornada_superendividamento:
    +engine: MergeTree()
    +order_by: tuple()
    prepared:
      +materialized: table
    indicators:
      +materialized: table
    consumption:
      +materialized: table

